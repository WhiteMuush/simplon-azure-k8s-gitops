apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: notes
  namespace: demo
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: managed-csi
  resources:
    requests:
      storage: 1Gi

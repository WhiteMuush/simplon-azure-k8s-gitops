apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: daily-demo
  namespace: velero
spec:
  schedule: "0 2 * * *"
  useOwnerReferencesInBackup: false
  template:
    includedNamespaces:
      - demo
    defaultVolumesToFsBackup: true
    ttl: 168h

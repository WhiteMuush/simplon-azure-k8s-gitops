apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: daily-demo
  namespace: velero
spec:
  schedule: "30 12 * * *"
  useOwnerReferencesInBackup: false
  template:
    includedNamespaces:
      - demo
      - database
    defaultVolumesToFsBackup: true
    ttl: 168h
